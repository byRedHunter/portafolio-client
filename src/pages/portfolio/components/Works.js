import React from 'react'

export const Works = () => {
	return (
		<section className='works'>
			<p className='text-error'>
				Estoy construyecto el back en Node, vuelve pronto.
			</p>
			{/* {listWork.map((work) => (
				<Work key={work._id} work={work} />
			))} */}
		</section>
	)
}
